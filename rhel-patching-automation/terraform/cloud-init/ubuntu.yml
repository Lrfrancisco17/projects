#cloud-config
packages:
  - python3

users:
  - name: ansible
    groups: [sudo]
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh-authorized-keys:
      - ${ssh_pubkey}

ssh_pwauth: false

runcmd:
  - hostnamectl set-hostname ubuntu-managed

